package com.pope.contract.dao.contract.extend;

import com.pope.contract.entity.contract.ContractAdd;

public interface ContractAddExtendMapper {
   
	int deleteByCondition(ContractAdd contractAdd);
}